# Disabled agents (wrong engine for this project)

TOMB RUSH is a React 19 + R3F + SpacetimeDB **web** game — it has no Unity / Unreal /
Godot engine, so these 15 engine-specialist agents were moved here to keep the active
roster relevant. They are NOT loaded by Claude Code from this folder. To re-enable any,
move the file back to `.claude/agents/`.
