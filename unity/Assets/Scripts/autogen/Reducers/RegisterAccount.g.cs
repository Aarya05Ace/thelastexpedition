// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void RegisterAccountHandler(ReducerEventContext ctx, string username, string pin);
        public event RegisterAccountHandler? OnRegisterAccount;

        public void RegisterAccount(string username, string pin)
        {
            conn.InternalCallReducer(new Reducer.RegisterAccount(username, pin));
        }

        public bool InvokeRegisterAccount(ReducerEventContext ctx, Reducer.RegisterAccount args)
        {
            if (OnRegisterAccount == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch(ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnRegisterAccount(
                ctx,
                args.Username,
                args.Pin
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class RegisterAccount : Reducer, IReducerArgs
        {
            [DataMember(Name = "username")]
            public string Username;
            [DataMember(Name = "pin")]
            public string Pin;

            public RegisterAccount(
                string Username,
                string Pin
            )
            {
                this.Username = Username;
                this.Pin = Pin;
            }

            public RegisterAccount()
            {
                this.Username = "";
                this.Pin = "";
            }

            string IReducerArgs.ReducerName => "register_account";
        }
    }
}
