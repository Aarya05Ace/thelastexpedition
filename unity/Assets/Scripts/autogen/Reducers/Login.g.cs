// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void LoginHandler(ReducerEventContext ctx, string username, string pin);
        public event LoginHandler? OnLogin;

        public void Login(string username, string pin)
        {
            conn.InternalCallReducer(new Reducer.Login(username, pin));
        }

        public bool InvokeLogin(ReducerEventContext ctx, Reducer.Login args)
        {
            if (OnLogin == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch(ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnLogin(
                ctx,
                args.Username,
                args.Pin
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class Login : Reducer, IReducerArgs
        {
            [DataMember(Name = "username")]
            public string Username;
            [DataMember(Name = "pin")]
            public string Pin;

            public Login(
                string Username,
                string Pin
            )
            {
                this.Username = Username;
                this.Pin = Pin;
            }

            public Login()
            {
                this.Username = "";
                this.Pin = "";
            }

            string IReducerArgs.ReducerName => "login";
        }
    }
}
